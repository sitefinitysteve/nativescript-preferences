# npm 11 exports a user-level allow-scripts setting into every script it runs, and then
# rejects it in the nested project-scoped installs the root scripts perform (EALLOWSCRIPTS).
# An empty value here overrides it for this repo; per-package install scripts are governed
# by each package.json allowScripts field instead.
allow-scripts=
